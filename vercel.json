{
    "framework": "vite",
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "env": {
        "GEMINI_API_KEY": "@gemini_api_key"
    },
    "functions": {
        "app/**/*.{js,ts}": {
            "runtime": "nodejs18.x"
        }
    }
}